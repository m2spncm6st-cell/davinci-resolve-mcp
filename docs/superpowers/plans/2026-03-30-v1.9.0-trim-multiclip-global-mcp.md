# v1.9.0 — Timeline Trim, Multi-Clip-Append, Global MCP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Drei unabhängige Erweiterungen: (A) Clips auf der Timeline trimmen, (B) mehrere Clips mit je eigenem Trim in einem Call einfügen, (C) MCP global via User-Scope registrieren.

**Architecture:** Alle Code-Änderungen ausschließlich in `src/server.py`. Feature C ist ein Shell-Befehl + CLAUDE.md-Update, kein Python-Code. Features A und B erweitern bestehende Compound-Tools mit neuen Actions/Parametern, rückwärts-kompatibel.

**Tech Stack:** Python 3.12, FastMCP v1.26, DaVinci Resolve Scripting API, pytest

---

## File Map

| File | Änderung |
|------|----------|
| `src/server.py` | Feature A: `timeline_item` Signatur + `trim` Action; Feature B: `media_pool` Signatur + `clip_specs` Logik |
| `tests/test_tools.py` | Neue Tests für Frames/Sekunden-Konvertierung und JSON-Parsing |
| `CLAUDE.md` | Status v1.9.0, Tool-Signaturen aktualisieren, Registrierungsbefehl auf `--scope user` |

---

## Task 1: Feature C — MCP User-Scope registrieren

**Files:**
- Modify: `CLAUDE.md` (Registrierungsbefehl aktualisieren)

- [ ] **Step 1: User-Scope registrieren**

```bash
claude mcp add davinci-resolve --scope user \
  -e PYTHONPATH="/Library/Application Support/Blackmagic Design/DaVinci Resolve/Developer/Scripting/Modules" \
  -e RESOLVE_SCRIPT_API="/Library/Application Support/Blackmagic Design/DaVinci Resolve/Developer/Scripting" \
  -e RESOLVE_SCRIPT_LIB="/Applications/DaVinci Resolve/DaVinci Resolve.app/Contents/Libraries/Fusion/fusionscript.so" \
  -- /Users/Alex/.pyenv/versions/3.12.11/bin/python3 /Users/Alex/Projects/davinci-resolve-mcp/src/server.py
```

Expected output: `Added MCP server davinci-resolve to user settings`

- [ ] **Step 2: Registrierung prüfen**

```bash
claude mcp list
```

Expected: `davinci-resolve` erscheint in der Liste mit `user` scope.

- [ ] **Step 3: CLAUDE.md aktualisieren — Registrierungsbefehl**

In `CLAUDE.md`, Abschnitt `## MCP-Registrierung`, den Befehl ersetzen mit:

```markdown
## MCP-Registrierung
```bash
claude mcp add davinci-resolve --scope user \
  -e PYTHONPATH="/Library/Application Support/Blackmagic Design/DaVinci Resolve/Developer/Scripting/Modules" \
  -e RESOLVE_SCRIPT_API="/Library/Application Support/Blackmagic Design/DaVinci Resolve/Developer/Scripting" \
  -e RESOLVE_SCRIPT_LIB="/Applications/DaVinci Resolve/DaVinci Resolve.app/Contents/Libraries/Fusion/fusionscript.so" \
  -- /Users/Alex/.pyenv/versions/3.12.11/bin/python3 /Users/Alex/Projects/davinci-resolve-mcp/src/server.py
```

- [ ] **Step 4: Commit**

```bash
cd ~/Projects/davinci-resolve-mcp
git add CLAUDE.md
git commit -m "chore: MCP User-Scope global registrieren, CLAUDE.md aktualisiert"
```

---

## Task 2: Feature A — Tests für `trim` Hilfsfunktion schreiben

**Files:**
- Modify: `tests/test_tools.py`

- [ ] **Step 1: Tests für Sekunden → Frame Konvertierung schreiben**

Am Ende von `tests/test_tools.py` einfügen:

```python
# ── trim helper logic tests ───────────────────────────────────────


def _seconds_to_frames(seconds, fps):
    """Replica of the conversion logic used in timeline_item trim."""
    return int(seconds * fps)


def _right_offset(end_frame, total_frames):
    """Replica of SetRightOffset calculation."""
    return total_frames - end_frame - 1


def test_trim_start_s_to_frame():
    assert _seconds_to_frames(2.0, 25.0) == 50
    assert _seconds_to_frames(0.0, 25.0) == 0
    assert _seconds_to_frames(1.5, 100.0) == 150


def test_trim_end_s_to_frame():
    assert _seconds_to_frames(10.0, 25.0) == 250
    assert _seconds_to_frames(3.0, 50.0) == 150


def test_trim_right_offset_calculation():
    # clip with 100 total frames, end_frame=79 → right_offset=20
    assert _right_offset(79, 100) == 20
    # end_frame = last frame → offset=0
    assert _right_offset(99, 100) == 0
    # end_frame = 0 → offset = total-1
    assert _right_offset(0, 100) == 99
```

- [ ] **Step 2: Tests laufen lassen — müssen PASS sein (reine Logik, kein Resolve)**

```bash
cd ~/Projects/davinci-resolve-mcp
python -m pytest tests/test_tools.py -v -k "trim"
```

Expected: 3 Tests PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/test_tools.py
git commit -m "test: Trim-Logik Tests (Sekunden→Frames, RightOffset-Berechnung)"
```

---

## Task 3: Feature A — `timeline_item` Signatur + `trim` Action implementieren

**Files:**
- Modify: `src/server.py:678-691` (Signatur), `src/server.py:692-742` (Docstring), `src/server.py:1044-1053` (neue Action + else-Block)

- [ ] **Step 1: `timeline_item` Signatur erweitern**

In `src/server.py`, die Funktionssignatur bei `def timeline_item(` (Zeile 678) ändern:

Aktuell:
```python
def timeline_item(
    action: str,
    track_type: str | None = None,
    track_index: int | None = None,
    item_index: int | None = None,
    property_name: str | None = None,
    property_value: str | None = None,
    clip_color: str | None = None,
    take_index: int | None = None,
    media_pool_clip: str | None = None,
    start_frame: int | None = None,
    end_frame: int | None = None,
    cache_type: str | None = None,
) -> dict:
```

Neu (drei Parameter ergänzen):
```python
def timeline_item(
    action: str,
    track_type: str | None = None,
    track_index: int | None = None,
    item_index: int | None = None,
    property_name: str | None = None,
    property_value: str | None = None,
    clip_color: str | None = None,
    take_index: int | None = None,
    media_pool_clip: str | None = None,
    start_frame: int | None = None,
    end_frame: int | None = None,
    cache_type: str | None = None,
    start_s: float | None = None,
    end_s: float | None = None,
    fps: float | None = None,
) -> dict:
```

- [ ] **Step 2: Docstring aktualisieren — `trim` Action + neue Parameter**

Im Docstring nach der Zeile mit `"set_speed": ...` (endet mit `100 = normal speed.`) die `trim` Action einfügen:

Aktuell (nach set_speed-Beschreibung, vor `Available properties`):
```
    - "set_speed": Set clip playback speed (retime). Requires: property_value (speed % as string, e.g. "25" for 25% = 4x slowmo, "50" for 2x slowmo, "200" for 2x fast).
      Optional: property_name ("ripple" = "true"/"false", default "false"). 100 = normal speed.

    Available properties for set_property:
```

Neu:
```
    - "set_speed": Set clip playback speed (retime). Requires: property_value (speed % as string, e.g. "25" for 25% = 4x slowmo, "50" for 2x slowmo, "200" for 2x fast).
      Optional: property_name ("ripple" = "true"/"false", default "false"). 100 = normal speed.
    - "trim": Set In/Out point of a clip already on the timeline. Requires at least one of:
      start_frame/start_s (source In-point) or end_frame/end_s (source Out-point).
      Optional: fps (auto-detected from clip if omitted). track_type, track_index, item_index to identify clip.
      start_frame/end_frame = absolute source frame numbers at native clip fps.
      start_s/end_s = seconds into source clip (converted to frames via fps).

    Available properties for set_property:
```

Und am Ende der Args-Sektion die neuen Parameter dokumentieren:

Aktuell:
```
        cache_type: Cache type "color" or "fusion" (for set_cache)
    """
```

Neu:
```
        cache_type: Cache type "color" or "fusion" (for set_cache)
        start_s: Source In-point in seconds (for trim)
        end_s: Source Out-point in seconds (for trim)
        fps: Native clip fps for seconds conversion (for trim, auto-detected if omitted)
    """
```

- [ ] **Step 3: `trim` Action implementieren**

In `src/server.py`, direkt VOR der abschließenden `else`-Klausel (die `return _err(f"Unknown action: {action}...")` enthält, aktuell nach `set_speed`), einfügen:

```python
    elif action == "trim":
        item, item_err = _get_item()
        if item_err:
            return item_err
        if start_frame is None and start_s is None and end_frame is None and end_s is None:
            return _err("trim requires at least one of: start_frame, start_s, end_frame, end_s")

        # Resolve fps: explicit > auto-detect from source clip
        clip_fps = fps
        if clip_fps is None:
            try:
                source = item.GetSourceItem()
                fps_str = source.GetClipProperty("FPS") if source else None
                clip_fps = float(fps_str) if fps_str else 25.0
            except Exception:
                clip_fps = 25.0

        # Convert seconds to frames
        sf = start_frame
        ef = end_frame
        if start_s is not None and sf is None:
            sf = int(start_s * clip_fps)
        if end_s is not None and ef is None:
            ef = int(end_s * clip_fps)

        applied_left = None
        applied_right = None

        if sf is not None:
            result = item.SetLeftOffset(sf)
            if result is False:
                return _err(f"SetLeftOffset({sf}) fehlgeschlagen — Offset außerhalb des Source-Clips?")
            applied_left = sf

        if ef is not None:
            try:
                source = item.GetSourceItem()
                total_str = source.GetClipProperty("Frames") if source else None
                total_frames = int(total_str) if total_str else None
            except Exception:
                total_frames = None

            if total_frames is None:
                return _err("Konnte Gesamtframe-Anzahl des Source-Clips nicht ermitteln — end_frame/end_s nicht anwendbar")

            right_offset = total_frames - ef - 1
            if right_offset < 0:
                return _err(f"end_frame {ef} liegt außerhalb des Source-Clips ({total_frames} Frames)")
            result = item.SetRightOffset(right_offset)
            if result is False:
                return _err(f"SetRightOffset({right_offset}) fehlgeschlagen — Offset außerhalb des Source-Clips?")
            applied_right = right_offset

        return _ok(
            item=item.GetName(),
            left_offset_set=applied_left,
            right_offset_set=applied_right,
            fps_used=clip_fps,
        )

```

- [ ] **Step 4: `trim` im Error-String der `else`-Klausel ergänzen**

Die abschließende `else`-Klausel in `timeline_item` (aktuell endet mit `"set_speed"`) um `"trim"` erweitern:

Aktuell:
```python
        return _err(
            f"Unknown action: {action}. Valid: get_current, get_properties, set_property, "
            "get_info, set_clip_color, clear_clip_color, set_enabled, get_source_info, "
            "get_takes, get_selected_take, add_take, select_take, delete_take, finalize_take, "
            "get_cache, set_cache, update_sidecar, stabilize, set_cdl, copy_grades, smart_reframe, "
            "set_speed"
        )
```

Neu:
```python
        return _err(
            f"Unknown action: {action}. Valid: get_current, get_properties, set_property, "
            "get_info, set_clip_color, clear_clip_color, set_enabled, get_source_info, "
            "get_takes, get_selected_take, add_take, select_take, delete_take, finalize_take, "
            "get_cache, set_cache, update_sidecar, stabilize, set_cdl, copy_grades, smart_reframe, "
            "set_speed, trim"
        )
```

- [ ] **Step 5: Syntax prüfen**

```bash
cd ~/Projects/davinci-resolve-mcp
python -m py_compile src/server.py && echo "OK"
```

Expected: `OK`

- [ ] **Step 6: Bestehende Tests laufen lassen**

```bash
python -m pytest tests/test_tools.py -v
```

Expected: alle Tests PASS.

- [ ] **Step 7: Commit**

```bash
git add src/server.py
git commit -m "feat: timeline_item action=trim — In/Out-Punkt von Timeline-Clips setzen"
```

---

## Task 4: Feature B — Tests für `clip_specs` JSON-Parsing schreiben

**Files:**
- Modify: `tests/test_tools.py`

- [ ] **Step 1: Tests für clip_specs JSON-Parsing schreiben**

Am Ende von `tests/test_tools.py` einfügen:

```python
# ── clip_specs JSON parsing tests ────────────────────────────────


import json as _json


def _parse_clip_specs(clip_specs_str):
    """Replica of the parsing logic used in media_pool append_to_timeline."""
    return _json.loads(clip_specs_str)


def _resolve_clip_frames(spec, clip_fps=25.0):
    """Replica of per-clip frame resolution logic."""
    sf = spec.get("start_frame")
    ef = spec.get("end_frame")
    start_s = spec.get("start_s")
    duration_s = spec.get("duration_s")
    fps = spec.get("fps", clip_fps)

    if start_s is not None and sf is None:
        sf = int(start_s * fps)
    if duration_s is not None and ef is None:
        base = sf if sf is not None else 0
        ef = base + int(duration_s * fps) - 1
    return sf, ef


def test_clip_specs_parse_valid_json():
    specs = _parse_clip_specs('[{"name": "clip1.mp4", "start_s": 10, "duration_s": 5}]')
    assert len(specs) == 1
    assert specs[0]["name"] == "clip1.mp4"


def test_clip_specs_parse_multiple_clips():
    raw = '[{"name": "a.mp4"}, {"name": "b.mp4", "start_frame": 0, "end_frame": 99}]'
    specs = _parse_clip_specs(raw)
    assert len(specs) == 2


def test_clip_specs_invalid_json_raises():
    try:
        _parse_clip_specs("not json")
        assert False, "Should have raised"
    except _json.JSONDecodeError:
        pass


def test_clip_specs_resolve_frames_from_seconds():
    sf, ef = _resolve_clip_frames({"start_s": 2.0, "duration_s": 3.0}, clip_fps=25.0)
    assert sf == 50
    assert ef == 50 + 75 - 1  # 124


def test_clip_specs_resolve_frames_direct():
    sf, ef = _resolve_clip_frames({"start_frame": 0, "end_frame": 249})
    assert sf == 0
    assert ef == 249


def test_clip_specs_no_trim():
    sf, ef = _resolve_clip_frames({"name": "clip.mp4"})
    assert sf is None
    assert ef is None


def test_clip_specs_per_clip_fps_override():
    sf, ef = _resolve_clip_frames({"start_s": 1.0, "duration_s": 1.0, "fps": 100.0})
    assert sf == 100
    assert ef == 199
```

- [ ] **Step 2: Tests laufen lassen**

```bash
cd ~/Projects/davinci-resolve-mcp
python -m pytest tests/test_tools.py -v -k "clip_specs"
```

Expected: 7 Tests PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/test_tools.py
git commit -m "test: clip_specs JSON-Parsing und Frame-Auflösung Tests"
```

---

## Task 5: Feature B — `media_pool` `clip_specs` Parameter + Logik implementieren

**Files:**
- Modify: `src/server.py:1060-1070` (Signatur), `src/server.py:1071-1101` (Docstring), `src/server.py:1244-1311` (append_to_timeline Branch)

- [ ] **Step 1: `media_pool` Signatur erweitern**

In `src/server.py`, die `media_pool` Signatur (Zeile 1060):

Aktuell:
```python
def media_pool(
    action: str,
    folder_name: str | None = None,
    file_paths: list[str] | None = None,
    timeline_name: str | None = None,
    start_frame: int | None = None,
    end_frame: int | None = None,
    duration_s: float | None = None,
    start_s: float | None = None,
    fps: float | None = None,
) -> dict:
```

Neu:
```python
def media_pool(
    action: str,
    folder_name: str | None = None,
    file_paths: list[str] | None = None,
    timeline_name: str | None = None,
    start_frame: int | None = None,
    end_frame: int | None = None,
    duration_s: float | None = None,
    start_s: float | None = None,
    fps: float | None = None,
    clip_specs: str | None = None,
) -> dict:
```

- [ ] **Step 2: Docstring für `append_to_timeline` aktualisieren**

Im Docstring von `media_pool`, die `append_to_timeline` Beschreibung erweitern:

Aktuell:
```
    - "append_to_timeline": Append clips from current folder to active timeline.
      Optional: file_paths (clip names to filter).
      Trim support: start_frame/end_frame (source frame numbers at native fps),
      OR start_s/duration_s (seconds, requires fps parameter or auto-detected from clip).
      Single clip per call when using trim. fps defaults to clip's native fps if omitted.
```

Neu:
```
    - "append_to_timeline": Append clips from current folder to active timeline.
      Optional: file_paths (clip names to filter).
      Trim support (single clip): start_frame/end_frame (source frame numbers at native fps),
      OR start_s/duration_s (seconds, requires fps parameter or auto-detected from clip).
      Multi-clip trim: clip_specs (JSON array, see below). Overrides file_paths/start_frame/etc.
      clip_specs format: '[{"name":"clip.mp4","start_s":10,"duration_s":5},{"name":"b.mp4"}]'
      Per-clip fields: name (required), start_frame, end_frame, start_s, duration_s, fps.
```

Und im Args-Abschnitt am Ende einfügen:

Aktuell:
```
        fps: Native clip fps for second-based calculations (auto-detected if omitted)
    """
```

Neu:
```
        fps: Native clip fps for second-based calculations (auto-detected if omitted)
        clip_specs: JSON array of clip dicts for multi-clip trim (overrides single-clip trim params)
    """
```

- [ ] **Step 3: `clip_specs` Logik in `append_to_timeline` Branch einfügen**

Im `elif action == "append_to_timeline":` Branch (ab Zeile 1244), direkt NACH dem bestehenden `if err: return err` Block und VOR dem `current = mp.GetCurrentFolder()` einfügen:

Aktuell (Zeilen 1244–1301):
```python
    elif action == "append_to_timeline":
        if err:
            return err
        # Append clips from current folder to the current timeline
        current = mp.GetCurrentFolder()
        clips = current.GetClipList() if current else []
        if not clips:
            return _err("No clips in the current folder")
        # If file_paths is given, use it as clip name filter
        if file_paths:
            selected = [c for c in clips if c.GetName() in file_paths]
            if not selected:
                return _err(f"No matching clips found. Available: {[c.GetName() for c in clips]}")
        else:
            selected = clips

        # Build clip infos — support trim via start_frame/end_frame or start_s/duration_s
        use_trim = (start_frame is not None or end_frame is not None or
                    start_s is not None or duration_s is not None)

        if use_trim:
            if len(selected) != 1:
                return _err("Trim (start_frame/end_frame/start_s/duration_s) requires exactly one clip — filter with file_paths")
            clip = selected[0]
            # Detect native fps from clip properties
            clip_fps = fps
            if clip_fps is None:
                fps_str = clip.GetClipProperty("FPS")
                try:
                    clip_fps = float(fps_str) if fps_str else 25.0
                except (ValueError, TypeError):
                    clip_fps = 25.0

            # Convert seconds to frames if needed
            sf = start_frame
            ef = end_frame
            if start_s is not None and sf is None:
                sf = int(start_s * clip_fps)
            if duration_s is not None and ef is None:
                base = sf if sf is not None else 0
                ef = base + int(duration_s * clip_fps) - 1

            clip_info = {"mediaPoolItem": clip}
            if sf is not None:
                clip_info["startFrame"] = sf
            if ef is not None:
                clip_info["endFrame"] = ef

            result = mp.AppendToTimeline([clip_info])
            if result is None or (isinstance(result, list) and len(result) == 0):
                return _err("AppendToTimeline failed — check if a timeline is active")
            return _ok(
                appended=1,
                clip_names=[clip.GetName()],
                start_frame=sf,
                end_frame=ef,
                clip_fps=clip_fps,
            )
        else:
            # AppendToTimeline returns a list of TimelineItems or empty list on failure
            result = mp.AppendToTimeline(selected)
            if result is None or (isinstance(result, list) and len(result) == 0):
                return _err("AppendToTimeline failed — check if a timeline is active")
            appended = result if isinstance(result, list) else [result]
            return _ok(
                appended=len(appended),
                clip_names=[c.GetName() for c in selected],
            )
```

Neu (vollständiger Ersatz des `elif action == "append_to_timeline":` Blocks):
```python
    elif action == "append_to_timeline":
        if err:
            return err

        # ── Multi-clip trim via clip_specs JSON ──────────────────
        if clip_specs is not None:
            import json as _json
            try:
                specs = _json.loads(clip_specs)
            except _json.JSONDecodeError as e:
                return _err(f"clip_specs ist kein gültiges JSON: {e}")

            current = mp.GetCurrentFolder()
            all_clips = current.GetClipList() if current else []
            clip_by_name = {c.GetName(): c for c in all_clips}

            clip_infos = []
            resolved = []
            for spec in specs:
                name = spec.get("name")
                if not name:
                    return _err(f"Jeder clip_specs-Eintrag benötigt 'name'. Ungültig: {spec}")
                clip = clip_by_name.get(name)
                if clip is None:
                    return _err(f"Clip '{name}' nicht gefunden. Verfügbar: {list(clip_by_name.keys())}")

                # Resolve fps: per-spec > auto-detect
                clip_fps = spec.get("fps")
                if clip_fps is None:
                    try:
                        fps_str = clip.GetClipProperty("FPS")
                        clip_fps = float(fps_str) if fps_str else 25.0
                    except (ValueError, TypeError):
                        clip_fps = 25.0

                sf = spec.get("start_frame")
                ef = spec.get("end_frame")
                ss = spec.get("start_s")
                ds = spec.get("duration_s")

                if ss is not None and sf is None:
                    sf = int(ss * clip_fps)
                if ds is not None and ef is None:
                    base = sf if sf is not None else 0
                    ef = base + int(ds * clip_fps) - 1

                info = {"mediaPoolItem": clip}
                if sf is not None:
                    info["startFrame"] = sf
                if ef is not None:
                    info["endFrame"] = ef
                clip_infos.append(info)
                resolved.append({"name": name, "start_frame": sf, "end_frame": ef, "fps_used": clip_fps})

            result = mp.AppendToTimeline(clip_infos)
            if result is None or (isinstance(result, list) and len(result) == 0):
                return _err("AppendToTimeline fehlgeschlagen — ist eine Timeline aktiv?")
            return _ok(appended=len(clip_infos), clips=resolved)

        # ── Bisheriger Single-Clip-Pfad (rückwärts-kompatibel) ───
        current = mp.GetCurrentFolder()
        clips = current.GetClipList() if current else []
        if not clips:
            return _err("No clips in the current folder")
        if file_paths:
            selected = [c for c in clips if c.GetName() in file_paths]
            if not selected:
                return _err(f"No matching clips found. Available: {[c.GetName() for c in clips]}")
        else:
            selected = clips

        use_trim = (start_frame is not None or end_frame is not None or
                    start_s is not None or duration_s is not None)

        if use_trim:
            if len(selected) != 1:
                return _err("Trim (start_frame/end_frame/start_s/duration_s) requires exactly one clip — filter with file_paths")
            clip = selected[0]
            clip_fps = fps
            if clip_fps is None:
                fps_str = clip.GetClipProperty("FPS")
                try:
                    clip_fps = float(fps_str) if fps_str else 25.0
                except (ValueError, TypeError):
                    clip_fps = 25.0

            sf = start_frame
            ef = end_frame
            if start_s is not None and sf is None:
                sf = int(start_s * clip_fps)
            if duration_s is not None and ef is None:
                base = sf if sf is not None else 0
                ef = base + int(duration_s * clip_fps) - 1

            clip_info = {"mediaPoolItem": clip}
            if sf is not None:
                clip_info["startFrame"] = sf
            if ef is not None:
                clip_info["endFrame"] = ef

            result = mp.AppendToTimeline([clip_info])
            if result is None or (isinstance(result, list) and len(result) == 0):
                return _err("AppendToTimeline failed — check if a timeline is active")
            return _ok(
                appended=1,
                clip_names=[clip.GetName()],
                start_frame=sf,
                end_frame=ef,
                clip_fps=clip_fps,
            )
        else:
            result = mp.AppendToTimeline(selected)
            if result is None or (isinstance(result, list) and len(result) == 0):
                return _err("AppendToTimeline failed — check if a timeline is active")
            appended = result if isinstance(result, list) else [result]
            return _ok(
                appended=len(appended),
                clip_names=[c.GetName() for c in selected],
            )
```

- [ ] **Step 4: `clip_specs` im Error-String der `else`-Klausel in `media_pool` ergänzen**

Die abschließende `else`-Klausel in `media_pool` (endet mit `append_to_timeline (with optional ...)`):

Aktuell:
```python
        return _err(
            f"Unknown action: {action}. Valid: list_folders, get_current_folder, set_current_folder, "
            "create_folder, delete_folder, list_clips, get_clip_info, import_media, "
            "create_timeline_from_clips, get_root_folder, selected_clips, append_to_timeline (with optional start_frame/end_frame/start_s/duration_s trim)"
        )
```

Neu:
```python
        return _err(
            f"Unknown action: {action}. Valid: list_folders, get_current_folder, set_current_folder, "
            "create_folder, delete_folder, list_clips, get_clip_info, import_media, "
            "create_timeline_from_clips, get_root_folder, selected_clips, "
            "append_to_timeline (single-clip: start_frame/end_frame/start_s/duration_s; multi-clip: clip_specs JSON)"
        )
```

- [ ] **Step 5: Syntax prüfen**

```bash
cd ~/Projects/davinci-resolve-mcp
python -m py_compile src/server.py && echo "OK"
```

Expected: `OK`

- [ ] **Step 6: Alle Tests laufen lassen**

```bash
python -m pytest tests/test_tools.py -v
```

Expected: alle Tests PASS.

- [ ] **Step 7: Commit**

```bash
git add src/server.py
git commit -m "feat: media_pool append_to_timeline mit clip_specs JSON für Multi-Clip-Trim"
```

---

## Task 6: CLAUDE.md auf v1.9.0 aktualisieren

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Status, Tool-Signaturen, Letzte Änderung aktualisieren**

In `CLAUDE.md`:

1. `## Status: v1.7.0 ...` → `## Status: v1.9.0 — Timeline Trim, Multi-Clip-Append, Global MCP`

2. In `### timeline_item(action, ...)` die Signatur erweitern:
```
### timeline_item(action, track_type, track_index, item_index, property_name, property_value, clip_color, take_index, media_pool_clip, start_frame, end_frame, cache_type, start_s, end_s, fps)
```
Und in der Actions-Liste ergänzen:
```
trim: Clip In/Out-Punkt setzen (start_frame/start_s, end_frame/end_s, fps auto-detect)
```

3. In `### media_pool(action, ...)` die Signatur erweitern:
```
### media_pool(action, folder_name, file_paths, timeline_name, start_frame, end_frame, duration_s, start_s, fps, clip_specs)
```
Und bei `append_to_timeline` ergänzen:
```
append_to_timeline: ... Multi-Clip: clip_specs (JSON-Array mit per-Clip name/start_frame/end_frame/start_s/duration_s/fps)
```

4. `## Phasenplan` — neue Phase ergänzen:
```
- [x] Phase 13: Timeline Trim, Multi-Clip-Append, Global MCP (v1.9.0)
```

5. `## Letzte Änderung` aktualisieren:
```
2026-03-30 — v1.9.0: timeline_item action=trim (SetLeftOffset/SetRightOffset), media_pool clip_specs Multi-Clip-Trim, MCP User-Scope global registriert
```

- [ ] **Step 2: Commit**

```bash
cd ~/Projects/davinci-resolve-mcp
git add CLAUDE.md
git commit -m "docs: CLAUDE.md auf v1.9.0 aktualisiert"
```

---

## Task 7: Git-Tag setzen

- [ ] **Step 1: Tag**

```bash
cd ~/Projects/davinci-resolve-mcp
git tag v1.9.0
git log --oneline -6
```

Expected: 6 Commits sichtbar (Tasks 1–6), Tag `v1.9.0` auf dem letzten.

---

## Spec-Coverage Check

| Spec-Anforderung | Task |
|-----------------|------|
| `timeline_item(action="trim")` mit start/end Frame+Sekunden | Task 3 |
| fps auto-detect für trim | Task 3 |
| SetLeftOffset / SetRightOffset | Task 3 |
| trim Error wenn keine Parameter | Task 3 |
| `clip_specs` JSON-Parameter in media_pool | Task 5 |
| Rückwärts-Kompatibilität alter append_to_timeline Pfad | Task 5 |
| Per-Clip fps-Override in clip_specs | Task 5 |
| MCP User-Scope `claude mcp add --scope user` | Task 1 |
| CLAUDE.md Registrierungsbefehl aktualisiert | Task 1 + Task 6 |
| Tests für Trim-Logik | Task 2 |
| Tests für clip_specs Parsing | Task 4 |
